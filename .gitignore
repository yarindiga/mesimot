# סודות — לעולם לא נכנסים למאגר
.env
users.json
keys.json

shared/_data.mjs

# אסימוני הכתיבה חיים במשתנה הסביבה APP_USERS ב-Netlify, לא בקוד.
# shared/_owners.mjs כן נכנס למאגר — הוא רק מיפוי משימה→מזהה אטום, בלי סודות.

# תוצרי בנייה מקומיים
node_modules/
*.pyc
__pycache__/
