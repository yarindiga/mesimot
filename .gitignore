# סודות — לעולם לא נכנסים למאגר
.env
users.json
keys.json

# shared/_data.mjs כן נכנס: Netlify מקמפלת ממנו את הפונקציה.
# הוא מכיל אסימוני כתיבה בלבד (סימון סטטוס), לא סיסמאות ולא תוכן משימות,
# ולכן המאגר חייב להישאר פרטי.

# תוצרי בנייה מקומיים
node_modules/
*.pyc
__pycache__/
